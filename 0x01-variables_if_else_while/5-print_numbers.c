#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point of our program
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int all_num;

	for (all_num = 0; all_num < 10; all_num++)
		printf("%d", all_num);
	printf("\n");
	return (0);
}
